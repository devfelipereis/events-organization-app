// require and configure dotenv, will load vars in .env in PROCESS.ENV
// require('dotenv').config();

const config = {
  env: 'development', // will be improved later
  port: 8080, // will be improved later
  // mongo: {
  //   host: // for later
  //   port: // for later
  // },
};

export default config;
